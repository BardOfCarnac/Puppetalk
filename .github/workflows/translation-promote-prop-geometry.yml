name: Promote translated prop geometry

on:
  push:
    branches: [puppetalk-translation]

permissions:
  contents: write

jobs:
  promote:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node --check translation/props/prop-geometry.js
      - run: node translation/props/prop-geometry-smoke.mjs
      - run: node translation/props/attachment-core-smoke.mjs
      - run: node translation/apply-prop-geometry-slice.mjs
      - run: node --check translation/build-runtime.mjs
      - run: node --check translation/runtime-parity-smoke.mjs
      - run: node translation/build-runtime.mjs
      - run: node --check translation/runtime/app.js
      - run: node translation/runtime-parity-smoke.mjs
      - run: node translation/entry-smoke.mjs
      - name: Commit promoted geometry runtime
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add translation/build-runtime.mjs translation/runtime-parity-smoke.mjs translation/runtime/app.js
          if git diff --cached --quiet; then
            echo 'Prop geometry runtime already current.'
            exit 0
          fi
          git commit -m 'Promote prop geometry into translated runtime'
          git push origin HEAD:${GITHUB_REF_NAME}
