name: Promote translated seat projection

on:
  push:
    branches: [puppetalk-translation]

permissions:
  contents: write

jobs:
  promote:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node --check translation/render/seat-projection.js
      - run: node translation/render/seat-projection-smoke.mjs
      - run: node --check translation/apply-seat-projection-slice.mjs
      - run: node translation/apply-seat-projection-slice.mjs
      - run: node --check translation/build-runtime.mjs
      - run: node --check translation/runtime-parity-smoke.mjs
      - run: node translation/build-runtime.mjs
      - run: node --check translation/runtime/app.js
      - run: node translation/runtime-parity-smoke.mjs
      - run: node translation/entry-smoke.mjs
      - run: node translation/render/seat-projection-smoke.mjs
      - name: Commit promoted seat projection runtime
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add translation/build-runtime.mjs translation/runtime-parity-smoke.mjs translation/runtime/app.js translation/index.html translation/entry-smoke.mjs
          if git diff --cached --quiet; then
            echo 'Seat projection runtime already current.'
            exit 0
          fi
          git commit -m 'Promote seat projection into translated runtime'
          git push origin HEAD:${GITHUB_REF_NAME}
