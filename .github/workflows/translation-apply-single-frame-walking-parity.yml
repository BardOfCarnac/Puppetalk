name: Apply single-frame walking parity

on:
  push:
    branches: [puppetalk-translation]
    paths:
      - 'translation/apply-single-frame-walking-parity.mjs'
      - '.github/workflows/translation-apply-single-frame-walking-parity.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: puppetalk-translation
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply single-frame walking correction
        run: node translation/apply-single-frame-walking-parity.mjs
      - name: Check parity script syntax
        run: node --check translation/live-browser-parity.mjs
      - name: Commit parity harness correction
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add translation/live-browser-parity.mjs
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "Prevent duplicate loops in walking parity"
          git push origin HEAD:puppetalk-translation
