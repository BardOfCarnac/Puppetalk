name: Apply preloaded walking probe

on:
  push:
    branches: [puppetalk-translation]
    paths:
      - translation/apply-preloaded-walking-probe.mjs
      - translation/preload-stage-walking-probe.txt
      - .github/workflows/translation-apply-preloaded-walking-probe.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply preloaded walking probe
        run: node translation/apply-preloaded-walking-probe.mjs
      - name: Check parity script syntax
        run: node --check translation/live-browser-parity.mjs
      - name: Commit preloaded walking probe
        run: |
          if git diff --quiet -- translation/live-browser-parity.mjs; then
            echo 'Preloaded walking probe patch produced no changes.'
            exit 1
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add translation/live-browser-parity.mjs
          git commit -m 'Preload stage walking engine probe'
          git push
