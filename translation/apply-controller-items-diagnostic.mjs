import fs from 'node:fs';

const path='translation/live-browser-parity.mjs';
let source=fs.readFileSync(path,'utf8');

const probeMarker='const stageWalkingProbeSource=';
const probe=`const controllerItemsProbeSource=String.raw\`(()=>{\n  let value;\n  Object.defineProperty(window,'PuppetalkControllerItems',{\n    configurable:true,enumerable:true,\n    get(){return value;},\n    set(next){\n      if(next?.create&&typeof next.create==='function'&&!next.create.__puppetalkParityProbe){\n        const raw=next.create;\n        const wrapped=function(...args){\n          const instance=raw.apply(this,args);\n          window.__PUPPETALK_PARITY_ITEMS__=instance;\n          return instance;\n        };\n        wrapped.__puppetalkParityProbe=true;\n        next={...next,create:wrapped};\n      }\n      value=next;\n    }\n  });\n})();\`;\n\n`;
if(!source.includes(probeMarker))throw new Error('Missing controller items probe insertion marker.');
source=source.replace(probeMarker,probe+probeMarker);

const targetFrom=`  await cdp.call('Page.addScriptToEvaluateOnNewDocument',{source:fakePeerSource});\n  if(stageProbe)await cdp.call('Page.addScriptToEvaluateOnNewDocument',{source:stageWalkingProbeSource});`;
const targetTo=`  await cdp.call('Page.addScriptToEvaluateOnNewDocument',{source:fakePeerSource});\n  await cdp.call('Page.addScriptToEvaluateOnNewDocument',{source:controllerItemsProbeSource});\n  if(stageProbe)await cdp.call('Page.addScriptToEvaluateOnNewDocument',{source:stageWalkingProbeSource});`;
if(!source.includes(targetFrom))throw new Error('Missing target injection marker.');
source=source.replace(targetFrom,targetTo);

const failFrom="  if(!pickupSend)throw new Error(`${label} frisbee pickup command was not emitted after refreshed moving-prop clicks.`);";
const failTo=`  if(!pickupSend){\n    const diagnostic=await evaluate(controller,\`(()=>{\n      const api=window.__PUPPETALK_PARITY_ITEMS__;\n      const geometry=(${latestGeometryExpression('__PROP_ID__')});\n      if(!api)return {api:false,geometry};\n      if(!geometry?.point)return {api:true,geometry};\n      const event={clientX:geometry.point.x,clientY:geometry.point.y};\n      const picked=api.pickTappedProp?.(event)||null;\n      const hand=picked?api.nearestPropHand?.(picked)||null:null;\n      return {\n        api:true,\n        picked:picked?{id:picked.id,type:picked.type,x:Number(picked.x),y:Number(picked.y),heldBy:picked.heldBy||null}:null,\n        hand,geometry\n      };\n    })()\`.replace('__PROP_ID__',${JSON.stringify('${propId}') }));\n    throw new Error(\`\${label} frisbee pickup command was not emitted after refreshed moving-prop clicks. DIAGNOSTIC \${JSON.stringify(diagnostic)}\`);\n  }`;
if(!source.includes(failFrom))throw new Error('Missing pickup failure marker.');
// Build the diagnostic expression without relying on helper closures inside the page.
const geometryBody=`(()=>{\n  const propId=__PROP_ID__;\n  const trace=window.__PUPPETALK_PARITY_TRACE__||[];\n  let prop=null;\n  for(let i=trace.length-1;i>=0&&!prop;i--){\n    const e=trace[i];\n    if(e.event!=='recv'||e.type!=='scene'||!e.scene)continue;\n    const q=e.scene.props?.find(p=>p.id===propId);\n    if(q)prop=q;\n  }\n  const r=document.querySelector('#personal-canvas')?.getBoundingClientRect();\n  if(!prop||!r)return null;\n  const source=window.PuppetalkSourceStage;\n  const sourceW=Number.isFinite(source?.width)&&source.width>100?source.width:320;\n  const sourceH=Number.isFinite(source?.height)&&source.height>100?source.height:360;\n  const camera=window.PuppetalkSceneCamera?.stageFrame?.(r.width,r.height)||null;\n  const sceneHasPhoto=!!(camera?.sceneId&&camera.sceneId!=='default');\n  const floorY=Number.isFinite(camera?.floorY)?camera.floorY:r.height*.88;\n  const floorLeft=sceneHasPhoto&&Number.isFinite(camera?.floorLeft)?camera.floorLeft:0;\n  const floorRight=sceneHasPhoto&&Number.isFinite(camera?.floorRight)?camera.floorRight:r.width;\n  const usableW=Math.max(r.width*.36,floorRight-floorLeft);\n  const topPad=Math.max(4,r.height*.018),bottomPad=Math.max(4,r.height*.018);\n  const scale=Math.max(.35,Math.min(usableW/sourceW,Math.max(.35,(floorY-topPad)/(sourceH*.90)),Math.max(.35,(r.height-bottomPad-floorY)/(sourceH*.10))));\n  const floorCenter=sceneHasPhoto?(floorLeft+floorRight)*.5:r.width*.5;\n  const offsetX=floorCenter-sourceW*scale*.5;\n  const offsetY=floorY-sourceH*.90*scale;\n  return {prop,point:{x:r.left+offsetX+prop.x*sourceW*scale,y:r.top+offsetY+prop.y*sourceH*scale},rect:{left:r.left,top:r.top,width:r.width,height:r.height},projection:{sourceW,sourceH,scale,offsetX,offsetY}};\n})()`;
const diagnosticBlock=`  if(!pickupSend){\n    const diagnostic=await evaluate(controller,\`(()=>{\n      const api=window.__PUPPETALK_PARITY_ITEMS__;\n      const geometry=\${${JSON.stringify(geometryBody)}.replace('__PROP_ID__',JSON.stringify(propId))};\n      if(!api)return {api:false,geometry};\n      if(!geometry?.point)return {api:true,geometry};\n      const event={clientX:geometry.point.x,clientY:geometry.point.y};\n      const picked=api.pickTappedProp?.(event)||null;\n      const hand=picked?api.nearestPropHand?.(picked)||null:null;\n      return {api:true,picked:picked?{id:picked.id,type:picked.type,x:Number(picked.x),y:Number(picked.y),heldBy:picked.heldBy||null}:null,hand,geometry};\n    })()\`);\n    throw new Error(\`\${label} frisbee pickup command was not emitted after refreshed moving-prop clicks. DIAGNOSTIC \${JSON.stringify(diagnostic)}\`);\n  }`;
source=source.replace(failFrom,diagnosticBlock);
fs.writeFileSync(path,source);
console.log('Added non-invasive controller item hit-test diagnostics to live parity.');

function latestGeometryExpression(){ return ''; }
